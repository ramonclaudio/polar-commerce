# Test Environment Configuration
# These values are used during testing with tools like Jest or Cypress
# Note: .env.local is NOT loaded in test environment for consistent test results

# Google Gemini API Configuration (Test)
# Use a test API key or mock key for testing
GOOGLE_GENERATIVE_AI_API_KEY=test_google_api_key_for_testing

# Application Base URL (Test)
# Use localhost or a test URL for testing
NEXT_PUBLIC_BASE_URL=http://localhost:3000